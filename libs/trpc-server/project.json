{
  "name": "@lunchbox/trpc-server",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/trpc-server/src",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "npm run test", "cwd": "libs/trpc-server" }
    }
  },
  "tags": ["scope:shared", "type:api"]
}
